GroupOfIssues : EB
{
	id, name, List<Issue>, organizationId, typeOfGroupOfIssuesId
}

TypeOfGroupOfIssues : EB
{
	id, name
}

Issue : EB
{
	id, name, status {id, name}, IssueContent, userId
}

IssueContent: EB 
{
	id, issueId, textContent, *comments, *attachments, *referencedIssues
}

statusy są per user, nawet jak się powtarzają
Status : EB
{
	id, name, organizationId == null
}

Flow to instrukacja w jaki sposob statusy w zadaniach moge się aktualizować
Flow : EB 
{
	id, name, organizationId == null, List<StatusInFlow>	
}

StatusInFlow : EB 
{
	id, statusId, Flow (wolałbym flowId (moze include samego id?)), List<AvailableStatusUpdate> avaibleStatusUpdates 
}

AvailableStatusUpdate : EB
{
	id, statusInFlowId, availableStatusId
}

Jak tworzyć?
Nowe flow: {todo, inprogress, complete}
request nowy status: {name: "inprogress", organizationId: "zTokenu"}
request na create Flow:
{
	name: "pierwsze flow", organizationId: "zTokenu", StatusInFlow: [{statusId:"statusId", availablestatuses:[{availableStatusId}, {availableStatusId}]}]
}

request na update flow:
{
	name, statusinflow: [{statusId, availablestatuses: [{availableStatusId}, {availableStatusId}]]
}
usuniecie statusu? - niemozliwe, gdy status jest przypisany do jakiegos flow


Uniwersalne flow: {todo, complete}